/// <reference types="vite/client" />

/**
 * Tipos del entorno de Vite.
 *
 * Hace falta para `import.meta.env.BASE_URL`, que la aplicación usa al construir
 * la ruta del catálogo curricular: en GitHub Pages la app cuelga de
 * /constelacion-educativa/ y una ruta absoluta no la encontraría.
 */
